# 参与贡献

感谢你考虑为这个算法模板集做出贡献！这是我个人的板子，公开出来希望能帮到大家。欢迎一起完善，共同学习进步。

## 如何贡献

### 报告问题

发现算法实现有 bug 或者模板有改进空间？

1. 到 [Issues](../../issues) 页面提交
2. 描述清楚问题：哪个章节、什么错误、期望的结果
3. 最好附带一个最小复现样例

### 提交改进

1. **Fork** 本仓库
2. **创建分支**：`git checkout -b fix/shortest-path`
3. **提交修改**，遵循下方的代码规范
4. **发起 Pull Request**，说明改了什么、为什么改

## 代码风格

所有贡献请遵循与现有模板一致的风格：

- **C++17/20** — 使用现代特性（`auto`、结构化绑定等）
- **自包含** — 每个代码块能独立编译
- **中英双语注释** — 保持现有风格
- **标注复杂度** — `// O(N log N)` 或等价注释
- **不用 `using namespace std`** 在可复用的代码块中，用 `std::` 限定
- **不可变性优先** — 创建新对象而非修改参数

## 模板格式

```markdown
### 算法名 (Algorithm Name)

// 中文 + 英文描述
// Time: O(N log N) | Space: O(N)

```cpp
#include <bits/stdc++.h>
// ... 实现
```
```

## 编译验证

提交前请确保代码能编译通过：

```bash
g++ -std=c++20 -O2 -Wall -Wextra -Wpedantic your_test.cpp -o test && ./test
```

## 行为准则

- 友善、尊重他人
- 就事论事，专注代码
- 帮助新人，一起进步

## 有疑问？

直接提 [Issue](../../issues) 或者开 [Discussion](../../discussions)，看到会回复。
